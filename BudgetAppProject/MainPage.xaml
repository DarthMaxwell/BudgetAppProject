<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:micro="clr-namespace:Microcharts.Maui;assembly=Microcharts.Maui"
             xmlns:local="clr-namespace:BudgetAppLibray;assembly=BudgetAppLibray"
    x:Class="BudgetAppProject.MainPage">

    <ScrollView>
        <VerticalStackLayout Margin="10,0" Spacing="25">
            <Grid ColumnDefinitions="*,Auto,Auto" Margin="0,5,0,0">
                <Picker
                    Grid.Column="0"
                    Margin="0,0,10,0"
                    ItemsSource="{Binding Profiles}"
                    ItemDisplayBinding="{Binding Name}"
                    SelectedItem="{Binding SelectedProfile}"
                    x:Name="ProfilePicker"
                    SelectedIndexChanged="ProfilePicker_SelectedIndexChanged"/>

                
                <Button 
                    Grid.Column="1"
                    x:Name="EditButton" 
                    Clicked="EditButton_Clicked" 
                    Text="Edit" 
                    Margin="0,0,5,0"/>

                <Button 
                    Grid.Column="2"
                    x:Name="AddButtonProfile" 
                    Clicked="AddButtonProfile_Clicked" 
                    Text="New Profile"/>
            </Grid>

            <micro:ChartView x:Name="ExpenseChart"
                         VerticalOptions="Center"
                         HorizontalOptions="Center"/>

            <CollectionView x:Name="DisplayExpenses">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="5" />
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="10" Margin="0,0,0,10">
                            <Grid ColumnDefinitions="Auto, *, Auto">
                                <BoxView Grid.Column="0" WidthRequest="20" HeightRequest="20" Color="{Binding DisplayColor}" VerticalOptions="Center" Margin="0,0,5,0"/>
                                <Label Grid.Column="1" Text="{Binding .}" VerticalOptions="Center" HorizontalOptions="Start"/>
                                <Label Grid.Column="2" Text="{Binding PercentageText}" VerticalOptions="Center" HorizontalOptions="End"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!--These should be hidden until hit the edit button-->
            <Entry x:Name="ProfileNameEntry" IsVisible="False" Text="{Binding SelectedProfile}"/>

            <CollectionView x:Name="Expenses" IsVisible="False" ItemsSource="{Binding SelectedProfile.Expenses}">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="10"/>
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border>
                            <Grid ColumnDefinitions="Auto, Auto, Auto, *, Auto" Padding="5">
                                <local:CustomSwitch1 IsToggled="{Binding isPercentage, Mode=TwoWay}" Grid.Column="0"/>

                                <Label Text="{Binding isPercentage, Converter={local:BoolToTextConverter}, Mode=OneWay}" Grid.Column="1" VerticalTextAlignment="Center"/>

                                <Entry Placeholder="Value" Text="{Binding Value}" Grid.Column="2"/>

                                <Entry Placeholder="Name"  Text="{Binding ExpenseName}" Grid.Column="3"/>

                                <Button Text="Delete" Clicked="DeleteButton_Clicked" Grid.Column="4"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!--Need a cancle button-->
            <Button x:Name="AddExpenseButton" Text="Add Expense" Clicked="AddExpenseButton_Clicked" IsVisible="False"/>
            <Button x:Name="RemoveProfileButton" Clicked="RemoveProfileButton_Clicked" IsVisible="False" Text="Remove Profile"/>
            <Button x:Name="SaveProfileButton" IsVisible="False" Clicked="SaveProfileButton_Clicked" Text="Save"/>
            <Button x:Name="CancelButton" IsVisible="False" Clicked="CancelButton_Clicked" Text="Cancel"/>
            
            <!--Will need a new profile button for this one-->
            <!--Can change the empty one to new profile and when its saved it needs a name and creates a new one-->
            

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
